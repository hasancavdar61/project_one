import 'package:flutter/material.dart';
import 'package:tidi_islam/view/favoriler/favoriler_widget.dart';

class FavorilerView extends StatelessWidget {
  const FavorilerView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const FavorilerWidget();
  }
}
